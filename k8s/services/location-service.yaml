apiVersion: apps/v1
kind: Deployment
metadata:
  name: location-service
  namespace: service-hub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: location-service
  template:
    metadata:
      labels:
        app: location-service
    spec:
      containers:
        - name: location-service
          image: YOUR_REGISTRY/service-hub-location-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8008
          envFrom:
            - configMapRef:
                name: service-hub-config
            - secretRef:
                name: service-hub-secrets
          env:
            - name: DB_NAME
              value: sh_location
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            tcpSocket:
              port: 8008
            initialDelaySeconds: 20
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            tcpSocket:
              port: 8008
            initialDelaySeconds: 60
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: location-service
  namespace: service-hub
spec:
  selector:
    app: location-service
  ports:
    - port: 80
      targetPort: 8008
